import React from 'react';

import {View, ImageBackground, Text} from 'react-native';

export default ({navigation}): React.ReactElement => {
    GLOBAL.show_log && console.log('Empty called!!!!');
    return <View></View>;
};
